{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install",
  "ignoreCommand": "for p in $(git rev-list --parents -n1 HEAD | cut -d' ' -f2-); do git diff --quiet \"$p\" HEAD -- . || exit 1; done"
}
